{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["**/dist/**", "**/.next/**"],
      "inputs": ["**/src/**"]
    },
    "lint": {
      "dependsOn": ["^lint"],
      "inputs": ["**/src/**"]
    },
    "format": {
      "dependsOn": ["^format"],
      "inputs": ["**/src/**"]
    }
  }
}